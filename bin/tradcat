#!/usr/bin/env bash
set -euo pipefail

if [ $# -eq 0 ] || [ "$1" = "-h" ] || [ "$1" = "--help" ]; then
  echo "Usage: tradcat \"text to translate\""
  echo ""
  echo "Translates text from any language to Catalan."
  echo "Outputs the translation and its IPA transcription."
  exit 0
fi

text="$*"

claude -p \
  --model haiku \
  "Translate the following text to Catalan. Output exactly two lines:
1. The Catalan translation
2. The IPA (International Phonetic Alphabet) transcription of the Catalan translation

No labels, no extra text, no explanations. Just the two lines.

Text: ${text}"
